gha-runner-scale-set-controller:
  flags:
    # Log level can be set here with one of the following values: "debug", "info", "warn", "error".
    # Defaults to "debug" in GitHubs helm chart used as the dependency.
    logLevel: "debug"
