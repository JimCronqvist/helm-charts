kubernetes-dashboard:
  serviceAccount:
    create: true

  nginx:
    enabled: false

  cert-manager:
    enabled: false

  metrics-server:
    enabled: false

  app:
    ingress:
      enabled: false # IngressRoute is used by this parent chart instead.
      hosts:
        - localhost
